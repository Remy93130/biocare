<form method="post" action="{{ path('acts_delete', {'id': act.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('Supprimer' ~ act.id) }}">
    <button class="btn">Delete</button>
</form>
