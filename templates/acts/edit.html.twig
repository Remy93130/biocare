{% set parent = 'base.html.twig' %}
{% if app.user.role.name == "MEDECIN" %}
    {% set parent = 'base_doctor.html.twig' %}
{% endif %}
{%extends parent %}

{% block title %}{{ parent() }}Edition Acts{% endblock %}

{% block body %}
	<div class="noselect" style="display:none">noselect</div>
	<div class="container-fluid" style="margin-top: 2%;">
		<div class="row">
		    <div class="col-lg-10">
		        <nav aria-label="breadcrumb">
		            <ol class="breadcrumb">
		                <li class="breadcrumb-item"><a href="{{ path('main_index') }}">Accueil</a></li>
		                <li class="breadcrumb-item"><a href="{{ path('doctor_acts') }}">Acte en cours</a></li>
		                <li class="breadcrumb-item active">Acte</li>
		            </ol>
		        </nav>
		    </div>
		</div>
        <div class="row">
            <div class="col-lg-8 offset-lg-1">
			    <h1>Editer Acts</h1>

			    {{ include('acts/_form.html.twig', {'button_label': 'Update'}) }} <br>

			    {{ include('acts/_delete_form.html.twig') }}

			    <a href="{{ path('doctor_acts') }}">Retour à la liste</a>
			</div>
		</div>
	</div>
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script src='{{ asset("js/acts_edit.js") }}'></script>
{% endblock %}