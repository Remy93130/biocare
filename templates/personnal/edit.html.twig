{% extends 'base.html.twig' %}

{% block title %}{{ parent() }}Modifier personnel{% endblock %}

{% block body %}
	<div class="container-fluid" style="margin-top: 2%;">
        <div class="row">
            <div class="col-lg-8 offset-lg-2">
			    <h1 class="text-center">Modifier personnel</h1>

			    {{ include('personnal/_form.html.twig', {'button_label': 'Update'}) }}

			    <a href="{{ path('personnal_index') }}">Retour à la liste</a>

			    {{ include('personnal/_delete_form.html.twig') }}
			</div>
		</div>
	</div>
{% endblock %}

{% block javascripts %}
	{{ parent() }}
	<script type="text/javascript">
		$("#personnal_password").prop("type", "hidden");
		$("label[for='personnal_password']").remove();
		$("#personnal_hospitalNode").prepend($('<option>', {value:null, text:''}));;
	</script>
{% endblock %}