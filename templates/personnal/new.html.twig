{% set parent = 'base.html.twig' %}
{% if app.user.role.name == "DATA_ADMIN" %}
    {% set parent = 'base_admin.html.twig' %}
{% endif %}
{%extends parent %}

{% block title %}{{ parent() }}Nouveau personnel{% endblock %}

{% block body %}
	<div class="container-fluid" style="margin-top: 2%;">
        <div class="row">
            <div class="col-lg-10">
			    <h1 class="text-center">Ajouter nouveau personnel</h1>

			    {{ include('personnal/_form.html.twig') }}

			    <a href="{{ path('personnal_index') }}">Retour à la liste</a>
			</div>
		</div>
	</div>
{% endblock %}
