{% extends 'base.html.twig' %}

{% block title %}Personnal{% endblock %}

{% block body %}
    <h1>Personnal</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ personnal.id }}</td>
            </tr>
            <tr>
                <th>Surname</th>
                <td>{{ personnal.surname }}</td>
            </tr>
            <tr>
                <th>Lastname</th>
                <td>{{ personnal.lastname }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ personnal.email }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ personnal.password }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('personnal_index') }}">back to list</a>

    <a href="{{ path('personnal_edit', {'id': personnal.id}) }}">edit</a>

    {{ include('personnal/_delete_form.html.twig') }}
{% endblock %}
