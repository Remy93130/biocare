<form method="post" action="{{ path('hospital_node_delete', {'id': hospital_node.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ hospital_node.id) }}">
    <button class="btn">Delete</button>
</form>
