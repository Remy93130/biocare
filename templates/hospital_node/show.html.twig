{% extends 'base.html.twig' %}

{% block title %}HospitalNode{% endblock %}

{% block body %}
    <h1>HospitalNode</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ hospital_node.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ hospital_node.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('hospital_node_index') }}">back to list</a>

    <a href="{{ path('hospital_node_edit', {'id': hospital_node.id}) }}">edit</a>

    {{ include('hospital_node/_delete_form.html.twig') }}
{% endblock %}
