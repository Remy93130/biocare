{% extends 'base.html.twig' %}

{% block title %}{{ parent() }}Structure{% endblock %}

{% block body %}
    <div class="container-fluid" style="margin-top: 2%;">
        <div class="row">
            <div class="col-lg-8 offset-lg-2">
                <h1 class="text-center">Structure</h1>
                {% include 'flashbag.html.twig' %}
                <table class="table">
                    <tbody>
                        <tr>
                            <th>Id</th>
                            <td>{{ hospital_node.id }}</td>
                        </tr>
                        <tr>
                            <th>Nom</th>
                            <td>{{ hospital_node.name }}</td>
                        </tr>
                        <tr>
                            <th>Responsable</th>
                            <td>{{ hospital_node.responsible.email }}</td>
                        </tr>
                    </tbody>
                </table>

                <a href="{{ path('hospital_node_index') }}">Retour à la liste</a> <br>

                <a href="{{ path('hospital_node_edit', {'id': hospital_node.id}) }}">Modifier</a>

                {{ include('hospital_node/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}
