{% extends 'base.html.twig' %}

{% block title %}{{ parent() }}Modifier structure{% endblock %}

{% block body %}
	<div class="container-fluid" style="margin-top: 2%;">
        <div class="row">
            <div class="col-lg-8 offset-lg-2">
			    <h1>Modifier une Structure</h1>

			    {{ include('hospital_node/_form.html.twig', {'button_label': 'Modifier'}) }}

			    <a href="{{ path('hospital_node_index') }}">Retour à la liste</a>

			    {{ include('hospital_node/_delete_form.html.twig') }}
			</div>
		</div>
	</div>
{% endblock %}
