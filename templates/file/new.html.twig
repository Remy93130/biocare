{% set parent = 'base.html.twig' %}
{% if app.user.role.name == "MEDECIN" %}
    {% set parent = 'base_doctor.html.twig' %}
{% endif %}
{%extends parent %}

{% block title %}{{ parent() }}Nouveau fichier{% endblock %}

{% block body %}
	<div class="container-fluid" style="margin-top: 2%;">
        <div class="row">
            <div class="col-lg-10">
			    <h1 class="text-center">Créer un nouveau fichier</h1>

			    {{ include('file/_form.html.twig') }}

			    {# <a href="{{ path('file_index') }}">Retour à la liste</a> #}
			</div>
		</div>
	</div>
{% endblock %}

{% block javascripts %}
	{{ parent() }}
	<script src='{{ asset("js/file_new.js") }}'></script>
{% endblock %}
