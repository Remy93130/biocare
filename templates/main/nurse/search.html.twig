{% extends 'base_nurse.html.twig' %}

{% block title %}{{ parent() }}Recherche{% endblock %}

{% block body %}
    <div class="container-fluid" style="margin-top: 2%">
        <div class="row">
            <div class="col-lg-8">
                <nav aria-label="breadcrumb">
                  <ol class="breadcrumb">
                    <li class="breadcrumb-item"><a href="{{ path('main_index') }}">Accueil</a></li>
                    <li class="breadcrumb-item active">Recherche DMP</li>
                  </ol>
                </nav>
                {% include("flashbag.html.twig") %}
            </div>
        </div>
        <div class="row">
            <div class="col-lg-6 offset-lg-1">
            <h1 class="text-center">Recherche de DMP</h1>
            <p class="text-justify">
                Pour rechercher un Dossier Médical Partagé (DMP), cliquer sur la liste déroulante.
                Vous pouvez ensuite rechercher le dossier en tapant le nom du patient et vérifier celui-ci avec
                son numéro de sécurité sociale.
            </p>
                <form method="get" action="{{ path("nurse_dmp_search") }}">
                    <div class="form-control">
                        <select name="dmp" class="custom-select custom-select-lg mb-3">
                            {% for dmp in dmps %}
                                <option value="{{ dmp.id }}">
                                    {{ dmp.surname }} {{ dmp.name }}
                                    - N° {{ dmp.socialNumber }}
                                </option>
                            {% endfor %}
                        </select>
                    </div><br>
                    <button class="btn btn-primary" type="submit">Confirmer</button>
                </form>
            </div>
        </div>
    </div>
{% endblock %}
